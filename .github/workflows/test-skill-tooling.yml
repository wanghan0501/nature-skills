name: Test skill tooling

on:
  pull_request:
    paths:
      - 'skills/nature-academic-search/mcp-server/**'
      - 'skills/nature-downloader/**'
      - 'skills/nature-image2ppt/**'
      - 'skills/nature-paper-card/**'
      - 'skills/nature-paper-to-patent/**'
      - 'skills/nature-response/**'
      - 'skills/nature-reviewer/**'
      - 'skills/nature-shared/**'
      - 'scripts/update-codex-skills.sh'
      - 'scripts/autoupdate-skills.sh'
      - 'scripts/tests/test-autoupdate-state-isolation.sh'
      - 'scripts/tests/test-update-codex-skills-safety.sh'
      - '.github/requirements-ci.txt'
      - '.github/workflows/test-skill-tooling.yml'
  push:
    branches:
      - main
    paths:
      - 'skills/nature-academic-search/mcp-server/**'
      - 'skills/nature-downloader/**'
      - 'skills/nature-image2ppt/**'
      - 'skills/nature-paper-card/**'
      - 'skills/nature-paper-to-patent/**'
      - 'skills/nature-response/**'
      - 'skills/nature-reviewer/**'
      - 'skills/nature-shared/**'
      - 'scripts/update-codex-skills.sh'
      - 'scripts/autoupdate-skills.sh'
      - 'scripts/tests/test-autoupdate-state-isolation.sh'
      - 'scripts/tests/test-update-codex-skills-safety.sh'
      - '.github/requirements-ci.txt'
      - '.github/workflows/test-skill-tooling.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  python-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: |
            .github/requirements-ci.txt
            skills/nature-academic-search/mcp-server/requirements.txt
            skills/nature-downloader/requirements.txt
            skills/nature-image2ppt/requirements.txt

      - name: Install Image2PPT system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends libreoffice-impress

      - name: Install test dependencies
        run: |
          python -m pip install \
            -c .github/requirements-ci.txt \
            pytest \
            -r skills/nature-academic-search/mcp-server/requirements.txt \
            -r skills/nature-downloader/requirements.txt \
            -r skills/nature-image2ppt/requirements.txt

      - name: Test nature-academic-search
        working-directory: skills/nature-academic-search/mcp-server
        run: python -m pytest -q tests

      - name: Test nature-downloader Python tooling
        working-directory: skills/nature-downloader
        run: python -m pytest -q tests/python

      - name: Test nature-image2ppt full Linux runtime
        working-directory: skills/nature-image2ppt
        run: python -m pytest -q tests

      - name: Test nature-paper-card
        working-directory: skills/nature-paper-card
        run: python -m pytest -q tests

      - name: Test nature-paper-to-patent
        working-directory: skills/nature-paper-to-patent
        run: python -m pytest -q tests

      - name: Test nature-response package consistency
        working-directory: skills/nature-response
        run: python -m pytest -q tests

      - name: Test nature-reviewer instruction contracts
        working-directory: skills/nature-reviewer
        run: python -m pytest -q tests

      - name: Test nature-shared consistency tooling
        working-directory: skills/nature-shared
        run: python -m pytest -q tests

  image2ppt-portable-core:
    name: Image2PPT portable core (${{ matrix.os }}, Python ${{ matrix.python }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            python: '3.10'
          - os: ubuntu-latest
            python: '3.12'
          - os: macos-latest
            python: '3.12'
          - os: windows-latest
            python: '3.12'
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: ${{ matrix.python }}
          cache: pip
          cache-dependency-path: skills/nature-image2ppt/requirements.txt

      - name: Install portable dependencies
        run: python -m pip install pytest -r skills/nature-image2ppt/requirements.txt

      - name: Test portable contracts without Office rendering
        working-directory: skills/nature-image2ppt
        run: >-
          python -m pytest -q
          tests/test_project_metadata.py
          tests/test_script_inventory.py
          tests/test_formula_renderer.py
          tests/test_console_portability.py
          tests/test_page_hints.py
          tests/test_pdf_backend.py
          tests/test_quality_contracts.py
          tests/test_path_confinement.py
          tests/test_visual_review_evidence.py

  javascript-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      - name: Test nature-downloader JavaScript tooling
        working-directory: skills/nature-downloader
        run: bun test tests/unit

  skill-sync-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Test skill install, verification, and drift detection
        env:
          TEST_DEST: ${{ runner.temp }}/nature-skills-sync-test
        run: |
          bash -n scripts/autoupdate-skills.sh
          bash -n scripts/update-codex-skills.sh
          bash scripts/tests/test-autoupdate-state-isolation.sh
          bash scripts/tests/test-update-codex-skills-safety.sh
          bash scripts/update-codex-skills.sh --dest "$TEST_DEST"
          bash scripts/update-codex-skills.sh --dest "$TEST_DEST" --check
          touch "$TEST_DEST/nature-writing/unexpected-drift-file"
          if bash scripts/update-codex-skills.sh --dest "$TEST_DEST" --check; then
            echo "Expected verification to detect destination drift."
            exit 1
          fi
