# Project-level Skill: copy this file to ./config.yaml and fill secrets locally.
# config.yaml is ignored by Git and must never be committed.
# Global/legacy installation: ~/.image2ppt/config.yaml remains supported.
# On Linux/macOS: chmod 600 ./config.yaml

# One Baidu AI Studio Access Token, not an API Key/Secret Key pair.
# Apply at https://aistudio.baidu.com/account/accessToken
PADDLE_OCR_TOKEN: ""

# Optional image-generation API fallback. Leave empty when using the agent image
# tool or Codex OAuth.
# OPENAI_API_KEY: ""
# OPENAI_BASE_URL: "https://api.openai.com/v1"
# IMAGE2PPT_IMAGE_MODEL: "gpt-image-2"
